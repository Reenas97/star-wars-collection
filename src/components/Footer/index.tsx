import styles from "./styles.module.css"

const Footer = () => {
    
    return (
        <footer className={`${styles.footer} container`}>
            <p>Desenvolvido por Renata Sanchez</p>
        </footer>
    )
}

export default Footer;